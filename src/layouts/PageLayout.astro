---
import BaseLayout from "./BaseLayout.astro";
import { site } from "../site.config";

interface Props {
  title?: string;
}

const { title = site.title } = Astro.props;
---

<BaseLayout title={title}>
  <div class="py-8 transition-slide-up">
    <slot />
  </div>
</BaseLayout>
