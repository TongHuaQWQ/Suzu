---
import BaseLayout from "./BaseLayout.astro";
import { site } from "../site.config";

interface Props {
  title?: string;
}

const { title = site.title } = Astro.props;
---

<BaseLayout title={title}>
  <div class="py-8 animate-fade-up">
    <slot />
  </div>
</BaseLayout>
